{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "ts build",
            "command": ["tsc"],
            "options": {
                "cwd": "${workspaceFolder}/src"
            }
        }
    ]
}